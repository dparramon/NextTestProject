{
	"version": "2.0.0",
	"tasks": [
        {
          "label": "Docker Build",
          "type": "shell",
          "command": "docker compose -f docker-compose.yml build",
          "group": "build"
        },
        {
          "label": "Docker Run",
          "type": "shell",
          "command": "docker compose -f docker-compose.yml up",
          "group": "build"
        }
	]
}